[package]
name = "skill_registry"
version = "0.1.0"
description = "SkillHive Marketplace — on-chain skill registry"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "skill_registry"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = "1.0.0"
anchor-spl = "1.0.0"
