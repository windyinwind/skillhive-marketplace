# SkillHive Marketplace API base URL
SKILLHIVE_MARKETPLACE_URL=http://localhost:3000

# Base58-encoded Solana private key for a funded devnet wallet.
# This wallet pays the on-chain registration fee.
# Generate one: solana-keygen new --outfile keypair.json && cat keypair.json
AGENT_WALLET_KEYPAIR=
